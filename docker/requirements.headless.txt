# requirements.headless.txt
# Dipendenze Python per la pipeline SENZA renderer (usato nel container Docker).
# Il renderer OpenGL gira sull'host direttamente.

networkx>=3.3
requests>=2.31.0
neo4j>=5.19.0
numpy>=1.26.0
